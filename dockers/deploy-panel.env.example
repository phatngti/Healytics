# deploy-panel.env — Written by deploy-panel.sh on each deployment.
# Committed example only. The real file is in .gitignore.
PANEL_IMAGE=nonameaaaa/healytics_panel
PANEL_TAG=replace-with-immutable-tag
PANEL_DEPLOYED_AT=
