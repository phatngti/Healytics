
networks:
  services:
    driver: bridge

# --- Kong Database Connection (shared by bootstrap + control-plane) ---
x-kong-db-config: &kong-db-env
  KONG_DATABASE: postgres
  KONG_PG_HOST: postgres
  KONG_PG_PORT: ${POSTGRES_PORT:-5432}
  KONG_PG_DATABASE: ${POSTGRES_DB:?POSTGRES_DB is required}
  KONG_PG_USER: ${POSTGRES_USER:?POSTGRES_USER is required}
  KONG_PG_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
  KONG_LICENSE_DATA: "${KONG_LICENSE_DATA:-}"

# --- Kong Network / Listener Config (control-plane only) ---
# Allow Admin API to be accessed without auth for local dev.
# For production, enforce RBAC and pass Kong-Admin-Token in the deck service.
x-kong-network-config: &kong-network-env
  KONG_ADMIN_LISTEN: "0.0.0.0:8001, 0.0.0.0:8444 ssl"

services:

  postgres:
    image: postgres:16
    container_name: docker_postgres
    ports:
      - '${POSTGRES_PORT:-5432}:5432'
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_DB: ${POSTGRES_DB}
    restart: always
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - services
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "${POSTGRES_USER}", "-d", "${POSTGRES_DB}"]
      interval: 5s
      timeout: 10s
      retries: 10

  postgres-init:
    image: postgres:16
    container_name: postgres-init
    networks:
      - services
    depends_on:
      postgres:
        condition: service_healthy
    restart: on-failure
    volumes:
      - ./init-databases.sh:/init-databases.sh:ro
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_EXTRA_DATABASES: ${POSTGRES_EXTRA_DATABASES:-}
    entrypoint: ["bash", "/init-databases.sh"]

  redis:
    image: redis:latest
    container_name: redis
    restart: always
    ports:
      - '${REDIS_PORT:-6379}:6379'
    command: >
      redis-server
        --appendonly yes
        --replica-read-only no
        --save 20 1
        --loglevel warning
        --requirepass ${REDIS_PASSWORD}
    volumes:
      - redisdata:/data
    networks:
      - services

  kong-bootstrap:
    image: '${GW_IMAGE:-kong/kong-gateway:3.12.0.2}'
    container_name: kong-bootstrap
    networks:
      - services
    depends_on:
      postgres-init:
        condition: service_completed_successfully
    restart: on-failure
    environment:
      <<: *kong-db-env
      KONG_PASSWORD: ${KONG_ADMIN_PASSWORD}
    command: kong migrations bootstrap

  kong-cp:
    image: '${GW_IMAGE:-kong/kong-gateway:3.12.0.2}'
    container_name: kong-cp
    restart: on-failure
    networks:
      - services
    environment:
      <<: [*kong-db-env, *kong-network-env]
      KONG_ADMIN_GUI_LISTEN: "0.0.0.0:8002, 0.0.0.0:8445 ssl"
      KONG_ADMIN_GUI_URL: http://${GW_HOST:-localhost}:8002
      KONG_PASSWORD: ${KONG_ADMIN_PASSWORD}
    depends_on:
      kong-bootstrap:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "kong", "health"]
      interval: 10s
      timeout: 10s
      retries: 10
    ports:
      - "8000:8000"
      - "8443:8443"
      - "8001:8001"
      - "8444:8444"
      - "8002:8002"
      - "8445:8445"

  # This service runs ONCE to apply your config
  kong-configurator:
    image: kong/deck:latest
    container_name: kong-configurator
    networks:
      - services
    volumes:
      - ./kong.yml:/kong.yml  # Mount your config file here
    environment:
      DECK_KONG_ADDR: http://kong-cp:8001
      # Use this if RBAC is enforced:
      # DECK_HEADERS: "Kong-Admin-Token:${KONG_ADMIN_PASSWORD}" 
    command: ["gateway", "sync", "/kong.yml"]
    depends_on:
      kong-cp:
        condition: service_healthy

  # This service runs ONCE to configure the Cloudflare Tunnel via API
  cloudflare-configurator:
    image: alpine:latest
    container_name: cloudflare-configurator
    networks:
      - services
    volumes:
      - ./cloudflare-tunnel-setup.sh:/setup.sh:ro
      - ./.env:/app/.env
      - tunnel-token:/tmp/tunnel-token
    entrypoint: ["sh", "-c", "apk add --no-cache curl jq >/dev/null 2>&1 && sh /setup.sh"]
    environment:
      CLOUDFLARE_API_TOKEN: ${CLOUDFLARE_API_TOKEN}
      CLOUDFLARE_ACCOUNT_ID: ${CLOUDFLARE_ACCOUNT_ID}
      CLOUDFLARE_ZONE_ID: ${CLOUDFLARE_ZONE_ID}
      CLOUDFLARE_TUNNEL_HOSTNAME: ${CLOUDFLARE_TUNNEL_HOSTNAME}
      CLOUDFLARE_TUNNEL_TOKEN: ${CLOUDFLARE_TUNNEL_TOKEN:-}
      CLOUDFLARE_TUNNEL_NAME: ${CLOUDFLARE_TUNNEL_NAME:-healytics-tunnel}
      CLOUDFLARE_TUNNEL_ORIGIN: ${CLOUDFLARE_TUNNEL_ORIGIN:-http://kong-cp:8000}
    depends_on:
      kong-cp:
        condition: service_healthy

  # Reads the tunnel token from the shared volume and writes it to an env file
  # that cloudflared can use. Falls back to CLOUDFLARE_TUNNEL_TOKEN from .env.
  cloudflare-token-reader:
    image: alpine:latest
    container_name: cloudflare-token-reader
    networks:
      - services
    volumes:
      - tunnel-token:/tmp/tunnel-token
    environment:
      CLOUDFLARE_TUNNEL_TOKEN: ${CLOUDFLARE_TUNNEL_TOKEN:-}
    entrypoint: ["sh", "-c"]
    command:
      - |
        # Prefer token from shared volume (written by cloudflare-configurator)
        if [ -f /tmp/tunnel-token/TUNNEL_TOKEN ] && [ -s /tmp/tunnel-token/TUNNEL_TOKEN ]; then
          echo "Using tunnel token from shared volume"
        elif [ -n "$${CLOUDFLARE_TUNNEL_TOKEN:-}" ]; then
          echo "Using tunnel token from .env"
          echo "$$CLOUDFLARE_TUNNEL_TOKEN" > /tmp/tunnel-token/TUNNEL_TOKEN
        else
          echo "ERROR: No tunnel token found. Run cloudflare-configurator first or set CLOUDFLARE_TUNNEL_TOKEN in .env"
          exit 1
        fi
    depends_on:
      cloudflare-configurator:
        condition: service_completed_successfully

  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: cloudflared
    restart: unless-stopped
    networks:
      - services
    command: ["tunnel", "--no-autoupdate", "run", "--token-file", "/tmp/tunnel-token/TUNNEL_TOKEN"]
    volumes:
      - tunnel-token:/tmp/tunnel-token:ro
    depends_on:
      cloudflare-token-reader:
        condition: service_completed_successfully

volumes:
  pgdata:
  redisdata:
  tunnel-token: